//Valid Variable Names:

 var userName    //(camelCase, starts with a letter)
var first_name   //(uses an underscore, valid)
var $price //(starts with $, valid)
var _id  //(starts with _, valid)
var total1   //(ends with a digit, valid)


// Invalid Variable Names:

var 123number    //(starts with a digit, invalid)
var my-variable    //(contains -, invalid)
var break    //(reserved keyword, invalid)
var user name    //(contains a space, invalid)
var @tag     //(starts with @, invalid)
